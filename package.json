{
  "name": "sec-plus-sprint",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start": "node server/index.js",
    "build": "npm install && npm run build --workspace=client",
    "client": "npm run dev --workspace=client",
    "server": "npm run dev --workspace=server"
  }
}
